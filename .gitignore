# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
dist/
out/
build/

# Environment files (never commit these)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Supabase local state
.supabase/
supabase/.temp/

# Self-hosted deployment: generated config + the SQLite database itself
# (scripts/setup.sh writes ./livekit.yaml with real secrets baked in; the
# db file and its WAL/SHM companions live under ./data)
/livekit.yaml
/data/

# Supabase -> SQLite data migration (issue #7): raw NDJSON table dumps and
# the auth-secrets staging file (password hashes, TOTP secrets) — real user
# data, never committed.
.migration-output/

# MCP config (contains project-specific refs)
.mcp.json

# OS
.DS_Store
Thumbs.db

# Claude
CLAUDE.md

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# TypeScript
*.tsbuildinfo

# Turbo cache
.turbo/
